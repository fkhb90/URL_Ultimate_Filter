name: SSOT Compiler Auto Build & Deploy

on:
  push:
    branches: [ main ]
    paths:
      - 'SSOT_Compiler.py'

# 宣告 GITHUB_TOKEN 的權限，允許推播程式碼與發佈 GitHub Pages
permissions:
  contents: write
  pages: write
  id-token: write

# 確保同一時間只有一個 Pages 部署任務在執行，避免並發衝突
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build-and-deploy:
    # 宣告部署環境為 github-pages，並自動獲取部署後的網址
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run SSOT Compiler and Matrix Tests
        run: |
          python SSOT_Compiler.py

      - name: Commit and Push Generated Artifacts
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # 將所有雙軌生成的 JS 與自動更新的 Markdown 日誌一併加入暫存區
          git add URL-Ultimate-Filter-Surge.js URL-Ultimate-Filter-Tampermonkey.user.js CHANGELOG.md
          
          # 建立本地的自動編譯 Commit，並加上 [skip ci] 防止觸發無限流水線迴圈
          git commit -m "Auto-build: Update JS filters and CHANGELOG [skip ci]" || echo "No changes to commit"
          
          # 在推播前，先將遠端最新進度拉取並進行 Rebase（變基）整合
          git pull --rebase origin main
          
          # 安全推送至遠端
          git push

      # 以下為 GitHub Pages 現代化部署的標準流程
      - name: Setup GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Report Artifact for Pages
        uses: actions/upload-pages-artifact@v3
        with:
          # 將 Python 生成的 public 資料夾作為靜態網站的根目錄上傳
          path: './public'

      - name: Deploy Report to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
